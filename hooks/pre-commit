#!/bin/sh

if ! just test; then
    echo "[PRE-COMMIT HOOK] - Commit rejected: 'just test' failed"
    echo "[PRE-COMMIT HOOK] - Fix the issues or use 'git commit --no-verify' to skip"
    exit 1
fi

exit 0
